<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:gravity="center_horizontal"
    android:orientation="vertical"
    android:paddingRight="15dp"
    android:paddingLeft="15dp"
    android:background="@color/content_background_theme">

    <Button
        android:id="@+id/btn_create_timetask"
        android:layout_width="match_parent"
        android:layout_height="48dp"
        android:layout_marginTop="10dp"
        android:layout_marginBottom="10dp"
        android:text="创建TimeTask"
        android:textColor="@color/content_background_theme"
        android:background="@drawable/btn_bg"/>

    <Button
        android:id="@+id/btn_create_alarm"
        android:layout_width="match_parent"
        android:layout_height="48dp"
        android:layout_marginTop="10dp"
        android:layout_marginBottom="10dp"
        android:text="创建Alarm"
        android:textColor="@color/content_background_theme"
        android:background="@drawable/btn_bg"/>

</LinearLayout>